# Saki 0.4.5 实施与发布记录

> 状态：已发布并冻结；更新：2026-09-17。
> [SPEC](./SPEC.md) · [IMPLEMENTATION](./IMPLEMENTATION.md) · [USER_GUIDE](./USER_GUIDE.md)

`[x]` 表示发布前完成，`[-]` 表示原计划但未纳入冻结发布证据；后者不是仍在进行的 0.4.5
任务。完整真机矩阵统一见 [1.0 发布前门槛](../../ROADMAP.md#10-发布前真机门槛)。

## P：规划

- [x] P0：确认已有 Claude adapter、安装器和基本真实 probe，确定复用范围。
- [x] P1：建立 0.4.5 规格、实施计划、任务和用户指南草案，接入文档索引与路线图。
- [x] P2：完成首轮文档检查，记录结果。
- [x] P3：继承已校准的 0.4 隐私契约，冻结范围、工作包依赖和验收判据。

## H：接入完善

- [-] H0：原计划确定完整 Claude Code 版本与事件能力矩阵；发布证据只记录实际受测范围。
- [-] H1：原计划补齐批准、拒绝、提问、工具失败、Stop 后继续、异常结束的完整状态回归。
- [-] H2：原计划扩展来源隔离、子事件过滤、缺失身份、乱序、重复事件与恢复边界验证。
- [-] H3：原计划扩展双作用域、配置冲突、路径空格及失效 wrapper 的诊断矩阵。
- [-] H4：原计划补齐 Claude 来源的 IPC、检查点与设备输出规则覆盖；
  文档对齐已完成，不把规则测试扩大解释为完整匿名化。
- [-] H5：原计划扩展 Host 停止、设备离线、IPC 不可用及身份异常的完整失败矩阵。

- [x] H6：未开始的 idle 不占屏；完成优先于 idle；无提交记录时按注意优先级选主项。

## B：已有接入证据

- [x] B0：2026-09-16 按用户要求安装本地 Claude Code 用户级 Saki hooks；安装器返回
  changed=true，随后 installed/wrapper_ready/identity_ready 均为 true，Host 服务运行。
  使用已有 0.4 开发实现，保留其他设置；未记录配置内容或本机身份。
- [x] B1：用户于 2026-09-16 确认已用本地 Claude Code 验证；按用户反馈记录接入验证，
  未提供逐事件明细，不扩大为完整批准/失败/并发或兼容矩阵通过。

B0 只证明当次安装及静态就绪，不覆盖卸载、其他作用域或真实客户端事件。

## V：冻结的验证记录

| 场景 | 必需证据 | 状态 |
| --- | --- | --- |
| 离线与安全回归 | `scripts/check.zsh`，新增用例和耗时边界 | 173 项测试通过记录 |
| Claude 基本生命周期 | 受测版本、真实开始/请求/工具/结束事件计数 | 用户本地确认；无逐事件扩展记录 |
| 批准、提问与失败 | 合成全覆盖，真实可控场景及未覆盖限制 | 0.4.5 未纳入发布证据 |
| 安装与卸载 | 临时配置保留其他设置；真实客户端加载结果 | 当次用户级安装与静态检查 |
| 混合多会话 | 真实 2 Codex + 2 Claude，独立状态/计时/焦点 | 迁入 1.0 真机矩阵 |
| 恢复与兼容 | Host 重启、会话 resume、版本降级与互通 | 迁入 1.0 真机矩阵 |
| 设备显示与链路 | 视觉/触摸、USB/BLE 恢复 | 迁入 1.0 真机矩阵 |

该表是 0.4.5 发布时的证据边界，不是当前待办。真实混合来源、设备、传输与资源项目已合并
到 1.0 候选的一次性真机矩阵；长 soak、睡眠唤醒和光学延迟仍为可选工程表征。

### 可执行的状态验收序列

所有序列使用合成身份和安全任务文本；真实测试只记录观察结果，不存原始报文。

| 序列 | 通过判据 |
| --- | --- |
| 开始 → 请求 → 工具开始/成功 → Stop | 登记 idle 但不占屏 → starting → working/thinking → completed；一轮独立计时 |
| 请求 → PermissionRequest → 批准后工具成功 | 等待期间 waiting_approval，后续工具结果清除等待；设备不代批 |
| 请求 → PermissionRequest → 拒绝 | 记录实际后续事件；工具失败不直接变整轮 failed，无后续事件不伪造完成 |
| 请求 → AskUserQuestion → 结果 | waiting_user 后恢复 thinking；未覆盖的其他输入通道明确列限制 |
| 工具失败 → 后续工具 → Stop | 失败摘要后允许继续，最后正常 completed |
| StopFailure / Stop 后继续 / SessionEnd | 分别验证 failed、同轮恢复、冻结与 60 秒清理，不混为取消 |
| 第二次请求 / resume / Host 重启 | 新请求计时归零且更新主项；resume 保留；重启冻结待确认再恢复 |
| 两来源各两会话交错更新 | 标题/状态/耗时互不串写，仅新有效请求切主项，其他事件不抢焦点 |

这些序列保留为后续回归输入；0.4.5 没有执行的真实来源和兼容组合不再作为本版本待关闭项。

## R：发布

- [x] R0：用户指南、已知限制和发布说明已归档。
- [x] R1：Host/固件正式版本统一为 0.4.5，release 包与校验文件已发布。
- [x] R2：v0.4.5 tag 和 GitHub Release 已发布；版本状态冻结。

## 本次规划验证

2026-09-16：本次仅修改规划文档和文档入口，`scripts/check.zsh` 通过：169 项测试、
Ruff 与 Git whitespace 检查通过。未运行真实 API、安装用户 hook 或操作设备；
该结果不代表 0.4.5 功能或真机验收通过。

2026-09-16（规划收敛）：已继承更新后的 0.4 隐私契约，移除文档不一致待办，保留规则和
Claude 数据流回归；补充本地安装证据、M1–M6 工作包、状态序列及四种兼容组合。
本轮 `scripts/check.zsh` 通过：169 项测试、Ruff 和 whitespace；仅修改文档，
未启动新的真实 API 或真机测试。当时仍按原计划推进，后续以本文件末尾的发布冻结记录为准。

2026-09-16（空闲会话筛选）：Host 已实现未提交且 idle 的记录不占主屏/侧栏；
完成/取消排在 idle 前，无提交记录时按注意优先级回退。沿用 Host total 和 60 秒终态 TTL，
空投影沿用设备“暂无会话”。`scripts/check.zsh` 173 项测试、Ruff 与 whitespace 通过，
新增两来源对称场景、TTL 边界、空投影 schema、旧检查点、resume、关闭和漏请求活动回归。
已重启本地 Host 应用修复；未烧录固件、调用真实 API 或完成屏幕视觉验收。

2026-09-16（提交决策）：用户确认本地 Claude Code 验证完成，明确要求不再进行回归并提交
当前工作。本次提交沿用此前 173 项测试通过的记录，不新增测试通过声明；其余未执行矩阵
保持原状态。待机页暂不修改。此次为本地开发提交，运行版本仍为 0.4.0-dev，
不代表 0.4.5 正式发布，不创建 tag、推送远端或生成 GitHub Release。

2026-09-16（tag 准备）：按用户要求准备 v0.4.5；Host、固件 release 与打包目标统一为
0.4.5，日常固件为 0.4.5-dev，并补充发布说明。沿用用户本地验证和此前测试记录，
本轮不运行回归或构建；当时尚未纳入证据的矩阵按原样记录，固件包及 GitHub Release 由随后
的发布步骤生成。

2026-09-17（发布记录冻结）：v0.4.5 已发布。上方 `[-]` 和矩阵中的未纳入项只描述发布时的
证据边界，不再作为 0.4.5 在制任务；完整真机、混合来源、恢复和传输矩阵统一迁入 1.0 候选。
