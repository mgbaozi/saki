# Saki 0.4.0 实施任务与验证

> 状态：核心实现及离线验证完成；真机集成、来源完整矩阵和发布待完成。
> 更新：2026-09-15
> [SPEC](./SPEC.md) · [IMPLEMENTATION](./IMPLEMENTATION.md) · [USER_GUIDE](./USER_GUIDE.md)

`[x]` 本项已验证，`[~]` 部分完成，`[ ]` 待完成。构建不等于硬件验收。

## G：决策与基线

- [~] G0：以 0.3 transport 为基线。0.3 已通过基础 USB/BLE doctor、首配与 K2 流程；
  其尚未关闭的稳定性/切换/Unity 真机项仍保留，见 [0.3 TASKS](../0.3.0/TASKS.md)。
- [~] G1：Codex/Claude 官方 hook 契约已核对；Claude Code 2.1.270 真实 API 单会话成功。
  两来源真实混合并发、批准/失败/中断全矩阵待完成；无可靠信号的限制已写规格。
- [x] G2：选定 capability + v1 sessions 单帧完整集合；放弃多帧事务和单项增量。
  当前共享 2+2 帧 1,382 B，最大转义文本保持 2,048 B，丢 ACK 精确重试与模式隔离已测。
- [~] G3：Host 32 / 设备 4 固定上限及静态构建通过；真机 UI、heap、栈及 BLE 延迟待测。

## P / H：协议与 Host

- [x] P0：新 schema、混合来源共享 fixture、协商/ACK/兼容说明与两端模型。
- [x] H0：显式 source、严格 SourceEvent、HMAC、64 KiB 输入、1 秒失败路径、4 KiB IPC。
- [x] H1：独立 run/时间线、旧 run 去重、Stop 后继续、工具失败不终结主任务、resume 不归零。
- [x] H2：32 项容量、受保护项、最新提交主项 + 3 项注意优先级侧栏、隐藏注意计数、TTL 与过期冻结。
- [x] H3：0600 原子检查点、损坏整体拒收、保留显示状态/run/修订/提交顺序/耗时、24 小时恢复窗口。
- [x] H4：完整投影冻结与 mailbox generation；慢 ACK 期间新事件保留、旧设备同一最新提交焦点。
- [x] H5：Codex / Claude Code adapter 及脱敏、子事件、批准通知限制的合成回归。
- [x] H6：可恢复配置备份、幂等 install/check/uninstall、保留用户 hooks/permissions/env，临时
  目录验证；跨项目 wrapper、静默失败、私有身份文件；本地 list/forget 显示管理。
- [ ] 高频输入与慢 BLE ACK 组合下的实际吞吐/心跳时间预算测量。

## F：固件

- [x] F0：固定完整显示集合、每项耗时/freshness、peer 候选与 UI 按值复制。
- [x] F1：原子提交、空集合、半帧、失败不推进 seq、精确重试、旧序号、跨 peer busy/grace。
- [x] F2 离线：mode 隔离、JSON 深度/NUL/尾随/UTF-8/长度保护及非法候选校验。
- [~] F3：最新提交主屏 + 三项颜色/小字侧栏、0/1 项全宽、总数/注意/FULL、临时查看与 15 秒返回已实现；
  真机中英文边界、点击区域、断线/BLE/K2 层与动画表现待验收。
- [x] F4 构建：dev、release、Unity 测试固件通过；未烧录或在目标板执行 Unity。

## I：尚未完成的必需联调

- [ ] I0：真实 2 Codex + 2 Claude Code，单来源各 4 项，以及两来源独立安装/关闭/重启。
- [ ] I1：四种 Host/固件版本组合端到端；0/1/2/4 项、满额、过期、同名、中英文与独立计时。
- [ ] I2：USB→BLE / BLE→USB 各 10 次，断线、丢 ACK、旧链路迟到、grace、Host/设备重启。
- [ ] I3：Unity 真机执行，之后刷回正常 dev/release 并恢复 Host。
- [~] I4：USB 四项 1 Hz 十分钟通过；BLE 短测试通过，十分钟测试按用户要求提前停止。release profile 运行时尚未测量。

用户已于 2026-09-15 明确授权烧录与真机验证，并手动进入 ROM 下载模式。
0.4.0-dev 已通过仓库脚本写入目标板，三个写入区域校验通过，应用已启动并握手确认版本。
USB 十分钟及 BLE 短测试通过；后续集成验收按用户要求暂停。可选 24 小时/10,000 次、睡眠唤醒和光学
延迟未执行，不作为已通过项，也不代替以上必需联调。

## R：文档与发布

- [x] R0：规格按最终单帧方案重写，用户指南、README/索引/路线图和开发发布说明同步。
- [x] R1 版本：Host 0.4.0.dev0，固件 dev 0.4.0-dev / release 0.4.0，封包目标一致。
- [ ] R1 候选包：尚未生成或安装 0.4 发布包，待硬件验收和版本状态确认。
- [ ] R2：发布门审查。未经用户明确要求不 commit/tag/push/创建 Release。

## 验证摘要

2026-09-14：Claude Code 2.1.270 真实 API probe 通过，6 个归一化事件、1 个 Session，包含
open/prompt/tool_start/tool_end/stop/close。只保留计数与类别；原始 hook、模型输出和凭据
未写入仓库或工具输出。真实批准、失败、取消和混合并发未覆盖。

2026-09-15：`scripts/check.zsh` 通过：152 项测试、Ruff 与 Git whitespace；原生固件测试直接编译实际 C
model/protocol/transport 与 ESP-IDF cJSON，ASan/UBSan 通过；同一事务契约编入 Unity。
新测试包含完整 4 项共享 fixture、半帧、重复/冲突重试、低优先级写入、grace、空清理，及
重复身份、容量、整数、来源、run、fresh、缺字段、深度、NUL、尾随和超长输入拒绝。

2026-09-15：dev/release 构建通过，镜像内容分别 1,327,797 / 1,197,339 B，DIRAM 静态余量
163,169 / 182,833 B。Unity 测试固件构建通过。上述为离线静态值，未测 0.4 板上最低 heap
或栈水位；0.3 的历史真机值不能当作 0.4 验收结果。构建产物和原始日志保持本地忽略。

2026-09-15：双 peer engine 与最后完整列表改用 PSRAM，回收约 23 KiB 内部静态 RAM，
之后 dev/release 重建通过。47 个修改/新增文件的凭据形态、私钥块和绝对用户主目录字面量
检查无命中；真实 API 未重复调用。

2026-09-15：新增 `scripts/test-device-sessions.py` 真机入口，复用现有协议与 USB/BLE
transport；合成 2+2 会话测试 0/1/2/4 项、精确/冲突重试、非法项、旧 clear、半帧和持续
1 Hz 更新，仅输出白名单 ACK/资源统计。不把协议 ACK 当作已完成视觉或触摸验收。

2026-09-15：动态确认唯一 ESP32-S3 ROM 端口后，`scripts/flash-firmware.zsh` dev 烧录成功，
bootloader、应用、既有分区表三处校验通过。未擦除整片 Flash、修改分区布局或清理 BLE bond。

2026-09-15：0.4.0-dev USB 真机四 Session 1 Hz 测试持续 600.2 秒、601 次更新；ACK
min/mean/P95/max 为 101.00/105.84/111.13/111.59 ms。内部 heap 最低 44,927 B，
app/UI/USB/BLE 栈最低为 1,996/7,208/4,848/6,560 B。发送丢失、心跳超时、UI 队列覆盖
增量为 0；3 个 invalid_frames 为主动注入的非法候选。并通过 0/1/2/4 项、重复/冲突重试、
旧 clear 隔离和半帧恢复。数据是合成 2 Codex + 2 Claude Code，不等同于真实客户端混合验收。

2026-09-15：BLE 同样的协议短测试通过，30.4 秒、31 次更新，ACK P95 418.48 ms、最大
419.11 ms，BLE 收发丢失为 0，BLE 任务栈最低 4,848 B。复用原有绑定，未清理 bond。

2026-09-15：用户要求先停止测试。已中断 BLE 十分钟持续测试并关闭连接，恢复常驻 Host；
未把中断的测试标记通过。双向切换及隔离 hook/Host 进程验证入口已实现，但本轮未运行。

2026-09-15（UI 调整）：按用户反馈将四行列表替换为最新提交主屏 + 右侧状态栏。
新增提交顺序持久化、主项保留、侧栏注意排序与临时选择策略。此前烧录和 USB/BLE 结果对应旧列表
布局；此次布局尚未烧录或进行真机视觉/触摸验证。按用户要求继续暂停真机与真实 API 测试。

本次离线回归：`scripts/check.zsh` 共 157 项通过，Ruff 与 whitespace 通过；新增主项保留、
提交顺序恢复、旧检查点迁移、重复请求不抢焦点、隐藏注意计数，以及实际 C 选择策略的
ASan/UBSan 回归。dev、release 和包含选择策略的 Unity 固件均构建通过（Unity 未上板运行）。

2026-09-16：用户进入 ROM 下载模式并再次明确要求烧录。动态确认唯一目标端口、停止 Host 后，
通过仓库脚本烧录最新主屏/侧栏布局的 dev 固件；应用写入 1,328,624 B，bootloader、应用与
既有分区表三个区域均通过哈希校验，烧录进程正常退出。已恢复 Host；烧录后仍枚举为下载模式，
待用户不按 K0、短按 RST 启动应用。此次仅完成烧录校验，未恢复长测试或调用真实 API，
新布局的真机视觉/触摸验收仍待完成。

2026-09-16（任务目标）：主屏 task.title 改为简短脱敏目标，优先 task_title，回退本地提取 prompt
短句；敏感行/代码/上下文排除，路径和网址占位，最长 48 字符/96 UTF-8 B。工具、Stop、resume
及“继续”不覆盖目标；按 Session 隔离并随检查点恢复。旧 IPC/检查点缺 goal 仍兼容。
线协议和固件未改变，无需重新烧录。本次使用合成用例验证，未调用真实 API。

任务目标修复验证：`scripts/check.zsh` 共 169 项通过，包含两来源目标到设备投影、工具/继续/
恢复保留、旧 IPC/检查点兼容、凭据/代码/上下文/路径过滤和 UTF-8 上限。Host 已停止后更新
当前已知任务的空目标检查点，再恢复常驻服务；只补目标，不改来源身份/状态/提交顺序。
服务运行确认通过，USB 收到新的 applied ACK。派生目标经本地 IPC、私有检查点传到设备，
常规同步日志不输出目标；未读取历史对话或调用模型。屏幕视觉效果仍以用户确认结果为准。

2026-09-16（隐私文档校准）：SPEC、IMPLEMENTATION、USER_GUIDE、Host README 和开发说明
统一描述任务目标的数据流与规则边界：读取 prompt/task_title 内存文本，派生短句进入 IPC、
0600 检查点及设备；短请求可原样成为标题，规则无法保证过滤所有路径或敏感内容。
补充 hook --stdout 含 goal 的诊断例外。上述校准细化此前“脱敏/不传原文”的简略表述，
历史合成测试仅证明所覆盖样例通过，不代表全部敏感内容均可识别。本次仅修改文档。
